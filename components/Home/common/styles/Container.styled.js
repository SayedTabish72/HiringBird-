import styled from "styled-components";

export const Container = styled.div`
  margin-inline: auto;
  width: min(90%, 90rem);
  padding: 2em 0;

  /* @media (max-width: 40em) {
    padding: 1em 0;
  } */
`;
